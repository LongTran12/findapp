import React from 'react'
// import data from '../data/data.json'
// import dataF from '../data/dataF.json'

export default function Filter() {
    // const list = (cate) => dataF.filter((e) => e.category === cate)
    // console.log(list('games'))
    // const fname = (name) => data.filter((e) => e.name === name)
    // console.log(fname('expo'))
    return (
        <div>
        </div>
    )
}
